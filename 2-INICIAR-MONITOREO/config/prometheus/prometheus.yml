global:
  scrape_interval: 15s

rule_files:
  - "/etc/prometheus/alert.rules.yml"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "azure_vm"
    static_configs:
      - targets: ["20.172.180.51:9100"] # <-- Pondremos la IP aquí después de crear la VM